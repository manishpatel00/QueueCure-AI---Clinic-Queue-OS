/**
 * Next-style compatibility entry.
 *
 * The hackathon brief references a page.tsx root, so this file re-exports the
 * Vite App component for documentation parity without changing the actual build
 * entry.
 */

export { default } from "../App";
